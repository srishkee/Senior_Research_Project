`data/:` Contains the lmdb database used by both models.

`caffe_experiments/:` Contains the AlexNet/GoogLeNet model configurations + scripts.

More information about each folder is provided in its subfolders. 